// This file defines the custom theme of the application

import { createTheme } from "@mui/material/styles";
export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#651fff",
    },
    secondary: {
      main: "#f50057",
    },
  },
});
